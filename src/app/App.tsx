import { Header } from '@/common/components/Header/Header';
import { Main } from './Main';

export const App = () => {
    return (
        <div>
            <Header />
            <Main />
        </div>
    );
};
