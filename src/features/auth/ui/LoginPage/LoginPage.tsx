import { LoginForm } from './LoginForm/LoginForm';
import { Title } from './Title/Title';

export const LoginPage = () => {
    return (
        <section>
            <Title />
            <LoginForm />
        </section>
    );
};
