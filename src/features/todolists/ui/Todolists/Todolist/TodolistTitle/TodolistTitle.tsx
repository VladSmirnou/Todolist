import { EditableSpan } from '@/common/components/EditableSpan/EditableSpan';

export const TodolistTitle = () => {
    return (
        <div>
            <EditableSpan />
            <button>x</button>
        </div>
    );
};
